package com.company;

public enum LexemeType {
    IDENTIFIER,
    STAR,
    FROM,
    AGGREGATE,
    BRACKET_LEFT,
    BRACKET_RIGHT,
    AS,
    COMMA,
    SELECT,
    JOIN,
    INNER,
    LEFT,
    RIGHT,
    ON,
    EQ,
    NE,
    GT,
    LT,
    GE,
    LE,
    AND,
    OR,
    TERMINAL
}
